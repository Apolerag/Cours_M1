int main(int argc, char *argv[])
{
	int quarante_deux;

	int *i, *j = i;
	quarante_deux = 0;

	while (1)
	if (quarante_deux >=42)
		quarante_deux++;
	else
		break;


	//free(i);
	return (quarante_deux != 0) && (3 == 0);
}
